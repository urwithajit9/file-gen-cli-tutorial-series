# fgen-series/Cargo.toml
[workspace]
members = [
    "part01",
    # "part02",  # add when ready
]
resolver = "2"

# Optional: shared dependencies
[workspace.dependencies]
# Add any dependencies used across parts
